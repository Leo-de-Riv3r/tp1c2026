# Output local del load test — no commitear los CSV / HTML generados
results*.csv
results*.html

# Excepción: el sample de una corrida real se commitea como evidencia
!results-sample.html

# Cache de bytecode de Python — generado por py_compile y por correr locust
__pycache__/
*.pyc

/dist/
/node_modules/

# Generado por scripts/generate-users.ts (se regenera en el entrypoint del contenedor)
users.json
