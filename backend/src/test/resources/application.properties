jwt.secret=test-only-secret-not-for-production-1234567890abcdef
jwt.expiration=31536000000

server.error.include-message=always

# CorsConfig exige FRONTEND_URI (sin default); en tests no hay .env ni env var → la definimos acá
FRONTEND_URI=http://localhost
