package com.tacs.tp1c2026;

// TODO: portar los tests de "agregar repetida / faltante" desde el branch feat/db-fixes.
//   En el branch dependían de `CardCollectionUserController` (POST /my-collection/...)
//   y de `ParticipationType` enum, ninguno de los cuales se portó a master.
//   Para reactivarlo hay que portar también CollectionService + CardCollectionUserController,
//   o reescribir los tests apuntando a los endpoints que sí existen
//   (`POST /api/users/{id}/collection`, `POST /api/users/{id}/missing-cards`).
public class CardsCollectionTests {
}
